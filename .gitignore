# Ignore all hidden dotfiles
.*

# Whitelist useful dotfiles
!/.editorconfig
!/.npmignore
!/.gitignore
!/.travis.yml

# Don't commit the dist directory
# that's where we build the icons
/dist

# Add a .gitkeep file in an empty
# directory to check it in the repository
!.gitkeep

# Ignore modules
/node_modules

# Ignore logs
*.log
